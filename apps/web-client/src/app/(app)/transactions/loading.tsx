import { RouteLoading } from '@/components/shell';

/** Skeleton of the activity screen, shaped like the list that is coming. */
export default function Loading() {
  return <RouteLoading rows={8} />;
}
