import { RouteLoading } from '@/components/shell';

/** Skeleton of the accounts list, shaped like the tiles that are coming. */
export default function Loading() {
  return <RouteLoading rows={3} />;
}
