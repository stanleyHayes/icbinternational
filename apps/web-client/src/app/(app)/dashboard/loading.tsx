import { RouteLoading } from '@/components/shell';

/** Skeleton of the home screen, shaped like the panels that are coming. */
export default function Loading() {
  return <RouteLoading rows={4} />;
}
